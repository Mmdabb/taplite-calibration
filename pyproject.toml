[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "taplite-calibration"
version = "0.2.0"
description = "Input preparation, screen-count ODME, and QVDF auto calibration for TAPLite4MPO"
readme = "README.md"
requires-python = ">=3.9"
license = { file = "LICENSE" }
keywords = [
  "transportation", "traffic-assignment", "odme", "calibration", "qvdf",
  "taplite", "travel-demand-model",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Operating System :: OS Independent",
  "Topic :: Scientific/Engineering",
]
dependencies = [
  "numpy>=1.21",
  "pandas>=1.4",
  "scipy>=1.7",
  "tomli>=2.0; python_version < '3.11'",
]

[project.optional-dependencies]
taplite = ["taplite4mpo>=0.3.0"]
test = ["pytest>=7"]
publish = ["build>=1", "twine>=5"]
all = ["taplite4mpo>=0.3.0", "pytest>=7", "build>=1", "twine>=5"]

[project.urls]
Homepage = "https://github.com/Mmdabb/taplite-calibration"
Repository = "https://github.com/Mmdabb/taplite-calibration"
Issues = "https://github.com/Mmdabb/taplite-calibration/issues"

[project.scripts]
taplite-calibration = "taplite_calibration.cli:main"
taplite-calibrate = "taplite_calibration.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
taplite_calibration = ["py.typed", "resources/*.toml"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
